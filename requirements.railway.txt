# Requirements for Railway deployment
# Updated to remove Windows-only dependencies

# Core dependencies
beautifulsoup4==4.12.3
lxml==5.3.0
selenium==4.27.1
webdriver-manager==4.0.2
openpyxl==3.1.4
pandas==2.1.4
python-dotenv==1.0.1
python-docx==1.2.0

# FastAPI and web server (for Railway)
fastapi==0.109.0
uvicorn[standard]==0.27.0
aiofiles==23.2.1
pydantic==2.5.3

# Testing (optional)
pytest==7.4.3

# Google Sheets export (optional)
gspread==5.12.0
oauth2client==4.1.3

# PDF conversion (cross-platform alternatives to pywin32)
# Option 1: WeasyPrint (HTML to PDF, no Office dependency)
weasyprint==61.0

# Option 2: For direct docx to PDF if needed (requires LibreOffice in Docker)
# uncomment if using LibreOffice method:
# unoconv==0.9.0

# HTTP requests (for better scraping)
requests==2.31.0
urllib3==2.1.0

